<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.backend.mapper.PartyMapper">
  <select
    id="selectNowPartyCnt"
    resultType="java.lang.Integer">
   SELECT COUNT(*)FROM party_participants WHERE party_participants.party_id=#{partyId} and party_participants.deleted=0;
    </select>
</mapper>
